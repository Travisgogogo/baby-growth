name: baby_growth
description: 宝宝成长记录
publish_to: 'none'
version: 1.8.3+42

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  intl: ^0.18.1
  fl_chart: ^0.65.0
  shared_preferences: ^2.2.2
  # image_picker: ^1.0.7  # 暂时禁用，依赖需要 SDK 35
  
  # 语音识别 (暂时禁用，CI构建问题)
  # record: ^5.0.0
  permission_handler: ^11.3.0
  http: ^1.2.0
  crypto: ^3.0.3
  
  # 本地通知
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/paraformer-zh-tiny.onnx