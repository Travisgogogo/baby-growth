name: baby_growth
description: 宝宝成长记录
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  intl: ^0.18.1
  fl_chart: ^0.65.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  
  # 语音识别
  onnxruntime: ^1.4.0
  record: ^5.0.4
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 禁用默认平台项目，使用自定义配置
  # 通过 GitHub Actions 生成平台项目